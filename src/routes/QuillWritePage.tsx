import Responsive from "components/common/Responsive";
import Editor from "components/quill/Quill";

const QuillWritePage = () => {
  return (
    <Responsive>
      <Editor />
    </Responsive>
  );
};

export default QuillWritePage;
